[package]
name = "DBCrack"
version = "0.1.5"
authors = ["Mason Sipe <m-sipe@protonmail.com>"]
edition = "2018"

[dependencies]
clap = {version = "~2.27.0", features = ["yaml"]}
rusqlite = {version = "0.24.0", features = ["bundled"]}
